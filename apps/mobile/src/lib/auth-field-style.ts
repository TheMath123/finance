/** Casca em pill dos campos das 5 telas de autenticação — cor de fundo neutra por padrão, borda vira `primary` só quando o campo está focado. Compartilhado entre os forms pra não repetir a mesma string 5 vezes. */
export const AUTH_FIELD_CLASSNAME =
  'h-14 rounded-full border-2 border-transparent bg-muted px-5 text-base focus:border-primary focus:bg-background';
